#general:
#  artifacts:
#    - "target" # files, relative to the build directory
#machine:
#  java:
#    version: oraclejdk8
#  environment:
#    UOM_VERSION: 0.8-SNAPSHOT
test:
   override:
     - mvn clean
#   post:
#     - cp $CIRCLE_TEST_REPORTS/*.* $CIRCLE_ARTIFACTS