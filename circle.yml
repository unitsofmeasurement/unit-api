#general:
#  artifacts:
#    - "target" # files, relative to the build directory
machine:
  java:
    version: oraclejdk6
#  environment:
#    UOM_VERSION: 0.8-SNAPSHOT
test:
   override:
     - mvn install
   post:
     - mvn deploy site:deploy -Dmaven.test.skip -s settings.xml